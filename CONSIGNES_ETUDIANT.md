# Consignes Étudiant — Git Arena

## 🎯 Votre mission (binôme)
- A = branche `feature/prenomA`
- B = branche `fix/prenomB`
- Faites **3 petits commits** chacun.
- Provoquez **1 conflit contrôlé** en modifiant la **même ligne** (titre `index.html` *ou* entrée `team.md`).
- Ouvrez une **Pull Request vers `dev`** (ou merge local).
- **Résolvez** le conflit proprement.

## ✅ Ce que l’on va vérifier
- Branches bien nommées
- Messages de commit **précis** (`feat:`, `fix:`, `docs:`…)
- Conflit résolu correctement (pas de délimiteurs restants)
- PR lisible avec description *template complété*

## 🛟 Aide-mémoire
- `git status` (votre meilleur ami)
- `git pull --rebase` avant de pusher si besoin
- Résolution dans VS Code : *Accept Current / Incoming / Both* (puis nettoyer)
